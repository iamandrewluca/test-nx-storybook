import React from 'react';

import './demo-lib.scss';

/* eslint-disable-next-line */
export interface DemoLibProps {}

export const DemoLib = (props: DemoLibProps) => {
  return (
    <div>
      <h1>Welcome to demo-lib!</h1>
    </div>
  );
};

export default DemoLib;
